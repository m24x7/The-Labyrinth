using UnityEngine;
using UnityEditor;

namespace GDD3400.Labyrinth
{
    /// <summary>
    /// This class provides a custom editor for the Throw Item component to visualize it in the scene view
    /// </summary>
    [CustomEditor(typeof(ThrowItem))]
    public class ThrowItemEditor : Editor
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
